# Build output — regenerated by build.py (locally or by the GitHub Actions
# workflow on every push). Never committed; GitHub Pages serves it directly
# from the workflow run, not from this repo.
/_site/
